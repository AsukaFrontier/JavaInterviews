数据库要点：
数据库基础概述，SQL（结构化查询语言），MySQL与InnoDB;

数据库基础概述：
1.事务
  满足ACID特性的一组操作，可以通过Commit提交一个事务，也可以使用Rollback进行回滚；
  ACID
    Atomicity原子性
    Consistency一致性
    Isolation隔离性
    Durability持久性
2.范式
  1NF：属性不可分
  2NF：每个非主属性完全函数依赖于键码
  3NF：非主属性不传递函数依赖于键码
  BCNF
  4NF
  5NF
3.并发一致性问题
  丢失修改
  脏读（无效数据读出：一个事务读取另外一个事务还没有提交的数据）
  不可重复读：在同一个事务中，两个相同的查询返回了不同的结果；
  幻读（重点在于数据条数变化：新增、删除）
4.事务隔离级别
  串行化Serializable
    所有事务一个接一个地执行（避免幻读）；
    对于基于锁来实现并发控制的数据库而言，串行化要求在执行范围查询的时候，需要获取范围锁；
  可重复读Repeated Read
    所有被Select获取的数据都不能被修改，可以避免一个事务前后读取数据不一致的情况；
    （无法避免幻读）
  读已提交Read Committed
    事务读取的时候获取读锁，在读完之后立即释放（不需要等事务结束）（写锁在事务提交之后才释放）；
    释放读锁之后，就可能被其他事务修改数据（该等级是SQL Server默认的隔离等级）；
  读未提交Read Uncommitted
    （最低的隔离等级）